export const roles = Object.freeze({
  USER: 'user', // default role
  ADMIN: 'admin',
  CUSTOMER: 'customer',
  AGENT: 'agent',
});

export const statuses = Object.freeze({
  ONLINE: 'online',
  BUSY: 'busy',
  AWAY: 'away',
  OFFLINE: 'offline',
});
